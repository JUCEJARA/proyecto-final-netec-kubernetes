version: '3.8'

services:
  mysql:
    container_name: mysql
    image: mysql:8.0
    environment:
      MYSQL_ROOT_PASSWORD: netect123
      MYSQL_DATABASE: db-medical
    ports:
      - "3306:3306"
    volumes:
      - ./data/mysql:/var/lib/mysql
    networks:
      - medical-app-network
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-u", "root", "-pnetect123"]
      interval: 10s
      timeout: 10s
      retries: 5
      start_period: 30s
  
  ms-medical-backend:
    container_name: ms-medical-backend
    build: .
    ports:
      - "8080:8080"
    environment:
      MYSQL_PASSWORD: netect123
    depends_on:
      mysql:
        condition: service_healthy
    networks:
      - medical-app-network

networks:
  medical-app-network:
    driver: bridge